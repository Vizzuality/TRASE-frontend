
import connect from 'connect';
// import { } from 'actions/flows.actions';
import Index from 'components/factsheets/index.component.js';

const mapMethodsToState = () => ({});

const mapViewCallbacksToActions = () => ({});

export default connect(Index, mapMethodsToState, mapViewCallbacksToActions);
